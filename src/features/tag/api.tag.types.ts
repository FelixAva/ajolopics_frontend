export interface GetTagResponseDTO {
  id: number;
  name: string;
  postCount: number;
}

export interface PostTagDTO {
  name: string;
}

export interface PostTagResponseDTO {
  id: number;
  name: string;
}
